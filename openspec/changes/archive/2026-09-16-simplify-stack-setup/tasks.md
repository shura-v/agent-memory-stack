## 1. Simplified configuration flow

- [x] 1.1 Remove service-selection, placement, service-origin, port, and service-interface questions; deliver an explicit Configure stack flow with fresh full-stack defaults, automatic publication of only Panel/MemoryProxy/MCP, private remaining interfaces, and preserved explicit .env topology.
- [x] 1.2 Keep real provider endpoint/key, model and remaining operational questions, navigation, and save-first behavior; deliver actionable .env guidance for incomplete advanced configurations and updated English prompt documentation.
- [x] 1.3 Generate missing local Core and CLIProxyAPI service keys automatically and reuse saved keys without keep/generate/manual prompts; preserve explicit .env keys, navigation stability, masked reviews, and the separate administrator/provider/remote credential flows.

## 2. Automatic ports during application

- [x] 2.1 Add runtime binding inspection and a temporary loopback publication helper using the existing runtime image; deliver default/saved-port preference, owned-listener reuse, engine-assigned fallback, distinct enabled mappings, and operation-scoped cleanup.
- [x] 2.2 Add sanitized classification of actual binding conflicts and bounded startup retries; deliver errors that distinguish busy ports from other engine/provider/image failures without exposing secrets.
- [x] 2.3 Move final network resolution before Compose/config generation and final applied-input capture; deliver persisted chosen ports, generated-origin provenance, matching derived URLs, and preserved previous applied settings across retries.

## 3. Documentation and related changes

- [x] 3.1 Document Panel/MemoryProxy/MCP-only default publication, automatic ports, and advanced .env overrides; connect resolved ports to the unchanged approved Connect an agent output.
- [x] 3.2 Reconcile MCP with the default three-listener policy and automatic ports; keep its protected Knowledge backend internal and suppress direct Knowledge HTTP tool instructions unless explicitly exposed. Deliver explicit Knowledge HTTP opt-in metadata and migration of legacy default host mappings without losing Knowledge data or memory/skill instructions.

## 4. Acceptance

- [x] 4.1 Build required images and start the configured containers; record image identities, runtime/platform, running long-lived services, and successful required initialization jobs. This is the sole current-stage acceptance check.

Comprehensive topology, port-conflict, agent, MCP, authorization, memory/Wiki, and recovery validation remains deferred until Supergateway and the remaining integrations are complete. Specification scenarios define required behavior rather than extra current-stage acceptance tasks.
